# Step 12b — business-logic as deterministic assertion predicate

**Status:** not started
**Maps to:** `REVISION_AI_SHAPE.md §10 step 12 row, §4.1 Pass-1 rule`
**Amends Phase 1 step:** 12
**Produces:** agent reshape at `src/agents/business-logic/`
**Depends on:** 08b, 17c
**Executed by:** `/new-agent` skill (amendment mode)
**Verification:** predicate-pure tests; fixed-checklist deterministic floor preserved; Hypothesis attachment happens in 18b Pass-2, not here

## Goal

Reshape business-logic into a deterministic assertion predicate. The fixed checklist from step 12 (self-approval, cross-tenant invitations, refund flows, admin enforcement, etc.) becomes the predicate set. AI's business-logic concerns arrive as Hypotheses and attach via Pass-2 in 18b. **This agent never reads `hypotheses.json`.**

## What lands

- `src/agents/business-logic/agent.ts` — reshape to `AssertionPredicate` shape per checklist item.
- `src/agents/business-logic/predicates/` — one predicate per checklist concern:
  - `business-self-approval.ts` — checks declared-context workflows for self-approval shapes; emits `coverage_gap + suggested_tests` when relevant action is declared but no negative test is observed.
  - `business-cross-tenant-invite.ts` — fires when declared-context includes invitation flows without observed tenant-isolation tests.
  - `business-refund-flow-authz.ts` — fires on declared refund flows without observed server-side approval evidence.
  - Other checklist items per step 12 spec.
- Each predicate is pure over `ScanFact[]` + `declared-context.json`. Output is `coverage_gap` or `missing_evidence` findings with `suggested_tests`.

## Done when

- Agent signature confirms `AssertionPredicate` shape.
- Agent NEVER emits `confirmed_issue` (assertion test). Promotion happens only via Phase 2 `proven_allowed`.
- Fixed-checklist behaviour is unchanged from step 12: same input declared-context → same coverage_gap findings.
- Hypotheses generated by AI Inference (08d) for business-logic concerns attach via Pass-2 (18b), not in this agent.

## Guardrails

- **Constraint 10:** facts-only.
- Per `REVISION_AI_SHAPE §4.5`: business-logic findings are never `confirmed_issue` unless backed by direct evidence — and the direct-evidence path is Phase 2 `proven_allowed` only.
- Per `output-language-lint`: suggested_tests use the §9 vocabulary.
- Per `FPP §2A`: checklist items are addressable by id (`business-self-approval`, etc.), enabling future checklist expansion without touching this agent's runtime code beyond adding new predicate files.

## References

- `REVISION_AI_SHAPE.md` §4.1, §10
- `phase-1/steps/12-agent-business-logic.md` (original — was not done; this reshape supersedes it)
- 08b, 17c
