{
  "swagger": "2.0",
  "info": {
    "version": "12.2.3",
    "title": "PostgREST"
  },
  "host": "0.0.0.0:3000",
  "basePath": "/",
  "schemes": ["http"],
  "consumes": ["application/json"],
  "produces": ["application/json"],
  "paths": {
    "/": { "get": { "tags": ["Introspection"], "summary": "OpenAPI description (this document)" } },
    "/orders": {
      "get": { "tags": ["orders"], "parameters": [], "responses": { "200": { "description": "OK" } } }
    },
    "/users": {
      "get": { "tags": ["users"], "parameters": [], "responses": { "200": { "description": "OK" } } }
    }
  },
  "definitions": {
    "orders": {
      "type": "object",
      "required": ["id"],
      "properties": {
        "id": { "type": "integer", "format": "bigint" },
        "user_id": { "type": "string", "format": "uuid" },
        "created_at": { "type": "string", "format": "timestamp with time zone" }
      }
    },
    "users": {
      "type": "object",
      "required": ["id"],
      "properties": {
        "id": { "type": "string", "format": "uuid" },
        "email": { "type": "string" }
      }
    }
  }
}
